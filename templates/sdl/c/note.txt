Makes a simple box using SDL.

Developing on Ubuntu.

To get the libs, do as mentioned on the sdl wiki:

sudo apt-get install libsdl2-2.0-0
sudo apt-get install libsdl2-dev

Run make to build.
Run with run.
